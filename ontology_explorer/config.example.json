{
  "_comment": "otlg CLI 配置示例 — 实际路径: ~/.otlg/config.json — 使用: mkdir -p ~/.otlg && cp config.example.json ~/.otlg/config.json",
  "backend": "rest",
  "api_url": "https://ontology-platform.example.com/api/v1",
  "api_key": "your-api-key-here"
}
